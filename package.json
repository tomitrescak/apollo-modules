{
  "name": "apollo-modules",
  "version": "0.0.1",
  "description": "Modular system for building apollo schemas",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "mocha src/*spec.ts --compilers ts:ts-node/register",
    "coverage": "istanbul cover _mocha -- --compilers js:babel-core/register \"dist/*_spec.js\" -R spec --timeout 3000",
    "coveralls": "istanbul cover _mocha --report lcovonly -- --compilers js:babel-core/register \"dist/**/*.spec.js\" -R spec --timeout 3000 && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "Apollo",
    "Graphql"
  ],
  "author": "Tomi Trescak",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.13.2",
    "chai": "^3.5.0",
    "graphql": "^0.6.2",
    "graphql-tools": "^0.6.4"
  },
  "devDependencies": {
    "mocha": "^3.0.2",
    "mocha-lcov-reporter": "^1.2.0",
    "chai": "^3.5.0",
    "coveralls": "^2.11.11",
    "ts-node": "^1.3.0",
    "typescript": "^1.8.10",
    "istanbul": "^1.1.0-alpha.1"
  }
}
